{
  "name": "hisaho-hp",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:seed": "wrangler d1 execute hisaho-hp-cms --remote --file=schema.sql",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
